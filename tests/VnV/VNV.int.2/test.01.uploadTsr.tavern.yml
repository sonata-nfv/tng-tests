#upload test suite result
test_name: upload trr
#
includes:
  - !include envfile.yml

stages:
 - name: upload_test
   request:
      url: "{vnv_packages:s}"
      method: POST
      files:
        package: "5gtango-test-package-example.tgo"
   response:
      status_code: 200
