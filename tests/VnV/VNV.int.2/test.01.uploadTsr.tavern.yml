#upload test suite result
test_name: upload trr
#
includes:
  - !include envfile.yml

stages:
  - name: upload_trr
    request:
      url: "{trr_host:s}"
      method: POST
      files:
        package: "test-descriptor-example.json"
    response:
      status_code: 200
     