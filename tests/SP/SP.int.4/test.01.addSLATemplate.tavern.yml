#upload sla template
test_name: upload sla template
#
includes:
  - !include envfile.yml

stages:
  - name: upload_sla_template
    request:
      url: "{upload_sla_template:s}"
      method: POST
      files:
        package: "sla_template_example.json"
    response:
      status_code: 200
     