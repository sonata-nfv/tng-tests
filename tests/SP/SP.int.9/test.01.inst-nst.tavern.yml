#get slices
test_name: instantiate_nst
#
includes:
  - !include envfile.yml

stages:
  - name: get_slices
    request:
      url: "{get_slices:s}"
      method: GET
    response:
      status_code: 200