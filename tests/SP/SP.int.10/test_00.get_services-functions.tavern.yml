#get_services
test_name: get services
#
includes:
  - !include envfile.yml

stages:
  - name: get_services
    request:
      url: "{get_services:}"
      method: GET
      headers:
        content-type: application/json
    response:
      status_code: 200

   
     
