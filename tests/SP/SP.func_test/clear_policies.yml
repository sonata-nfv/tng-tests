#upload sla template
test_name: upload sla template
#
includes:
  - !include envfile.yml

stages:
  - name: get_policies
    request:
      url: "{upload_policy:s}"
      method: GET
      headers:
        content-type: application/json
    response:
      status_code: 200
      save:
        body:
          uuid: 0.uuid
  - name: delete_policy
    request:
      url: "{upload_policy:s}/{uuid:s}/"
      method: DELETE
    response:
      status_code: 200