#upload sla template
test_name: upload sla template
#
includes:
  - !include envfile.yml

stages:
  - name: get_packages
    request:
      url: "{upload_package:s}"
      method: GET
      headers:
        content-type: application/json
    response:
      status_code: 200
      save:
        body:
          uuid: 0.uuid
  - name: delete_package
    request:
      url: "{upload_package:s}/{uuid:s}/"
      method: DELETE
    response:
      status_code: 204
