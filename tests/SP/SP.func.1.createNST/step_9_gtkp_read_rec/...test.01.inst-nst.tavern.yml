#get slices
test_name: instantiate_nst
#
includes:
  - !include envfile.yml

stages:
  - name: get_services
    request:
      url: "{get_services:s}"
      method: GET
    response:
      status_code: 200
     