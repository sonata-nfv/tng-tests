#upload package
test_name: get packages
#
includes:
  - !include envfile.yml

stages:
  - name: get_packages
    request:
      url: "{upload_package:s}"
      method: GET
      headers:
        Content-Type: application/json
    response:
      status_code: 200
   
     
