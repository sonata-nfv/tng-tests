#upload package
test_name: upload package
#
includes:
  - !include envfile.yml

stages:
  - name: upload_package
    request:
      url: "{upload_package:s}"
      method: POST
      files:
        package: "5gtango-ns-package-example.tgo"
    response:
      status_code: 200
   
     
