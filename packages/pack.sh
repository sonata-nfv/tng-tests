#!/bin/bash
set -e
# directly call the validator (to make error pasing simple)
#tng-validate --project tng-smpilot-ns1-emulator -t

# package all projects
#tng-pkg -p tng-smpilot-ns1-emulator --skip-validation
echo "I am now packaging all the packages ... implement me!"